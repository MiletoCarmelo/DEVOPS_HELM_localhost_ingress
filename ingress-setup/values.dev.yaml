environment: dev
domain: mm.ch
infos:
  email: quant-cm@gmail.com
service:
  port: 80
  targetPort: 80
  nodePort: 30007
  selector:
    app: success-app
ingress:
  enabled: true
  annotations:
    clusterIssuer: "selfsigned"
    ingressClass: "nginx"
    nginx: 
      sslRedirect: "false"
      backendProtocol: "HTTP"
      # whiteListSourceRange: "127.0.0.1"
  hosts:
    paths:
      - path: /
        pathType: Prefix
  tls:
    - secretName: quant-cm-tls-dev
      certificateName: quant-cm-tls-dev