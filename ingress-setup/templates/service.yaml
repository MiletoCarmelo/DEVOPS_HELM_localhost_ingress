# templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: quant-cm-service
  namespace: {{ .Vales.environment }}
spec:
  # type: ClusterIP
  type: NodePort
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      # protocol: TCP
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: {{ .Values.service.selector.app }}