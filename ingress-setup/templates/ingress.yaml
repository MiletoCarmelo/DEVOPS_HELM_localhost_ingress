apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: central-ingress  # Un seul ingress centralisé
  namespace: {{ .Values.environment }}
  labels:
    {{- include "ingress-setup.labels" . | nindent 4 }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  tls:
    - secretName: {{ .Values.tls.secretName }}
      hosts:
        {{- range .Values.services }}
        - {{ include "ingress-setup.hostname" (dict "subdomain" .subdomain "baseDomain" $.Values.baseDomain) }}
        {{- end }}
  rules:
    {{- range .Values.services }}
    - host: {{ include "ingress-setup.hostname" (dict "subdomain" .subdomain "baseDomain" $.Values.baseDomain) }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .service.name }}
                port:
                  number: {{ .service.port }}
    {{- end }}