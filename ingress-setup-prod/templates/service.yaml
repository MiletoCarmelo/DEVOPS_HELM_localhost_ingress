apiVersion: v1
kind: Service
metadata:
  name: quant-cm-service
spec:
  selector:
    app: quant-cm
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}